// divide Function Expression
const divide = function(a, b) {
    return a / b;
  }
  
  // square Arrow Function
  const square = x => x * x;
  
  // add Arrow Function
  const add = (a, b) => a + b;
  
  